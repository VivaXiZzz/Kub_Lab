// 4.cpp : Этот файл содержит функцию "main". Здесь начинается и заканчивается выполнение программы.
//

#include <iostream>
using namespace std;

int main()
//{//определить совершенное число
//    int a, b = 0;
//    cin >> a;
//    for (int i = 1;i < a;i++) {
//        if (a % i == 0)b += i;
//    }
//    if (a == b) cout << "yep";
//    else cout << "nop";
//}

//{// opr yavl li chisla druj
//	int a, b, b1=0, a1=0;
//	cin >> a >> b;
//	for (int i = 1;i < a;i++) {
//		if (a % i == 0)a1 += i;
//	}
//	for (int i = 1;i < b;i++) {
//		if (b % i == 0)b1 += i;
//	}
//	if (a == b1 && a1 == b) cout << "druj";
//	else cout << "not druj";
//}

//{ // sred arifm veshestv na chet pos
//	int n,a=0;
//	float c,b=0;
//	cin >> n;
//	for (int i = 1;i <= n;i++) {
//		cin >> c;
//		if (i % 2 == 0) b += c;
//	}
//	c = b / (n/2);
//	cout << c;
//}

//{// sum n chisel %2 i stoyachih na pos %3
//	int n,a, b = 0;
//	cin >> n;
//	for (int i = 1;i <= n;i++) {
//		cin >> a;
//		if (i % 3 == 0 && a % 2 == 0)b += a;
//	}
//	cout << b;
//}

//{// sum chisel<0
//	int a,c,b = 0;
//	cin >> a;
//	for (int i = 0;i < a;i++) {
//		cin >> c;
//		if (c < 0)b += c;
//	}
//	cout << b;
//}

//{ //sum chisel >=0 i okanch na 6
//	int a, b, c = 0;
//	cin >> a;
//	for (int i = 0;i < a;i++) {
//		cin >> b;
//		if (b > 0 && b % 10 == 6)c += b;
//	}
//	cout << c;
//}

//{// *chet elem
//	int a, b, c = 1;
//	cin >> a;
//	for (int i = 1;i <= a;i++) {
//		cin >> b;
//		if (b % 2 == 0)c *= b;
//	}
//	cout << c;
//}

//{//sred arifm polosh elem
//	int a, b, c = 0;
//	cin >> a;
//	for (int i = 0;i < a;i++) {
//		cin >> b;
//		if (b >= 0)c += b;
//	}
//	cout << c;
//}

//{// sum vseh ok na 15
//	int a, b, c = 0;
//	cin >> a;
//	for (int i = 0;i < a;i++) {
//		cin >> b;
//		if (b % 100 == 15 || b % 100 == -15)c += b;
//	}
//	cout << c;
//}

//{//n chisel sum chisel u kotorih posled>pedposled
//	int a, b, c = 0;
//	cin >> a;
//	for (int i = 0;i < a;i++) {
//		cin >> b;
//		if (b % 100 / 10 < b % 10)c += b;
//	}
//	cout << c;
//}

//{//n chisel colvo chisel u kotorih proizved posled chisel chetno
//int a, b, c = 0;
//cin >> a;
//for (int i = 0;i < a;i++) {
//	cin >> b;
//	if (((b% 100 / 10) * (b % 10))%2 == 0)c++;
//}
//cout << c;
//}
